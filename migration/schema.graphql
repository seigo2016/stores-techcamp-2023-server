type Item {
    xid: String! @id
    name: String!
    preview: String
    price: Int!
}

type User {
    xid: String! @id
    name: String!
    boughtItems: [Item]
    orders: [Order] 
}

type Order {
    boughtItems: [Item]
}