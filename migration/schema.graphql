type Item {
    name: String! @id
    shop: String!
    price: Int!
    users: [User]
    orders: [Order]
}

type User {
    name: String! @id
    boughtItem: [Item]
    orders: [Order]
}

type Order {
    boughtItem: [Item] 
}